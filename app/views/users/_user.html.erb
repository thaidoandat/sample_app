<li>
  <%= gravatar_for user, Settings.avatar.size.for_list %>
  <%= link_to user.name, user %>
  <% if current_user.admin? && !current_user?(user) %>
    | <%= link_to t(".delete"), user, method: :delete, data: {confirm: t(".confirm")} %>
  <% end %>
</li>
